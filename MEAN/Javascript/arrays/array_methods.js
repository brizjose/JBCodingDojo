// js makes available a number of methods for arrays, accessible with dot notation after the array name

const hobbies = ['Music', 'Coding'];

// map

const hobbyObject = hobbies.map(hobby => 'Hobby: ' + hobby);

console.log(hobbyObject);

// reference

// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array#